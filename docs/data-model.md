# Модель данных

## 1. Словарь проекта

| Термин курса | Термин моей темы |
|---|---|
| Ticket | финансовая операция |
| Site | статья бюджета (справочник) |
| User | бухгалтер / аналитик / финансовый директор |

## 2. Сущности и поля

### 2.1. BudgetItem (справочник статей бюджета)

| Поле | Тип | Ключ |
|---|---|---|
| id | int | PK |
| name | string | — |
| type | string | — |

### 2.2. FinancialOperation (главный объект — финансовая операция)

| Поле | Тип | Ключ |
|---|---|---|
| id | int | PK |
| number | string | — |
| title | string | — |
| description | string | — |
| amount | decimal | — |
| status | string | — |
| budgetItemId | int | FK -> BudgetItem.id |
| createdByUserId | int | FK -> User.id |
| assigneeUserId | int? | FK -> User.id, может быть пустым |

### 2.3. User (пользователь)

| Поле | Тип | Ключ |
|---|---|---|
| id | int | PK |
| login | string | — |
| fullName | string | — |
| role | string | — |

## 3. Связи

- один справочник (BudgetItem) — много финансовых операций;
- один пользователь создаёт много операций;
- один пользователь может быть назначен на много операций;
- у новой операции исполнитель может отсутствовать.

## 4. Проверка 3НФ

Название статьи бюджета хранится в сущности BudgetItem один раз. FinancialOperation содержит внешний ключ budgetItemId. При переименовании статьи меняется одна строка, поэтому текст названия не дублируется в операциях.

## 5. Роли и права

| Роль | Создаёт | Назначает | Меняет статус |
|---|---|---|---|
| Бухгалтер | да | да | нет |
| Аналитик | нет | нет | да (только свою) |
| Финансовый директор | нет | да | нет |
